# create_bullet_error() generates the correct error messages

    Code
      create_bullet_error(idx, top_message, bullet_prefix, bullet_suffix,
        extra_message)
    Condition
      Error in `create_bullet_error()`:
      ! Can't find branch lengths for all the branches of the input tree.
      x Branch 1 does not have branch length.

---

    Code
      create_bullet_error(idx, top_message, bullet_prefix, bullet_suffix,
        extra_message)
    Condition
      Error in `create_bullet_error()`:
      ! Can't find branch lengths for all the branches of the input tree.
      x Branch 2 does not have branch length.
      x Branch 3 does not have branch length.

---

    Code
      create_bullet_error(idx, top_message, bullet_prefix, bullet_suffix,
        extra_message)
    Condition
      Error in `create_bullet_error()`:
      ! Can't find branch lengths for all the branches of the input tree.
      x Branch 1 does not have branch length.
      x Branch 2 does not have branch length.
      x Branch 3 does not have branch length.
      x Branch 4 does not have branch length.
      x Branch 5 does not have branch length.
      ... and 5 more branches do not have branch lengths.

---

    Code
      create_bullet_error(idx, top_message, bullet_prefix, bullet_suffix,
        extra_message)
    Condition
      Error in `create_bullet_error()`:
      ! Can't find branch lengths for all the branches of the input tree.
      x Branch 1 does not have branch length.
      x Branch 3 does not have branch length.
      x Branch 4 does not have branch length.

---

    Code
      create_bullet_error(idx, top_message, bullet_prefix, bullet_suffix,
        extra_message)
    Condition
      Error in `create_bullet_error()`:
      ! Can't find branch lengths for all the branches of the input tree.
      x Branch 1 does not have branch length.
      x Branch 3 does not have branch length.
      x Branch 4 does not have branch length.
      x Branch 9 does not have branch length.
      x Branch 11 does not have branch length.
      ... and 2 more branches do not have branch lengths.

